@echo off
rem Template: LiveData + ViewModel (AndroidX)
rem Add the following dependencies to your build.gradle (app module):
rem implementation "androidx.lifecycle:lifecycle-viewmodel-ktx:2.8.0"
rem implementation "androidx.lifecycle:lifecycle-livedata-ktx:2.8.0"
rem Then create a ViewModel class extending ViewModel and expose LiveData for biometric state.

echo Added LiveData & ViewModel dependencies.
